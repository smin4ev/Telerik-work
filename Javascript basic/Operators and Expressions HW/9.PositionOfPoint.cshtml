<!DOCTYPE html>
<html>
    <head>
        <title>Position of Point</title>
    </head>
    <body>
        <div id="js-console">
            <div>
                <label for="abs">Enter a X Coordinate</label>
                <input type="text" id="abs" />
            </div>
            <div>
                <label for="ord">Enter a Y Coordinate</label>
                <input type="text" id="ord" />
            </div>
            <div>
                <label>Click the button to check if point(x;y) is withint the circle K(O, 5) </label>
                <input type="button" value ="Check" onclick ="CheckPositionOfPoint()" />
            </div>
        </div>
         <script src="scripts/js-console.js"></script>
        <script>
            //9.Write an expression that checks for given point (x, y) if it is within the circle K( (1,1), 3) 
            //and out of the rectangle R(top=1, left=-1, width=6, height=2).
            function CheckPositionOfPoint() {
                var abs = jsConsole.read("#abs");
                var ord = jsConsole.read("#ord");
                var sum = Math.sqrt(((abs + 1) * (abs + 1)) + ((ord + 1) * (ord + 1)));
                if (((sum) <= 3) && ((abs > 1) || (abs < -1) || (ord < -1) || (ord > 5))) {
                    jsConsole.writeLine('For this input coordinates point is in the cicrcle and outside of the rectangle!');
                }
                else {
                    jsConsole.writeLine('For this input coordinates point is outside of the circle or in the rectangle! ')
                }
            }
        </script>
    </body>
</html>
