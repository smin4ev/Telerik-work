<!DOCTYPE html>
<html>
    <head>
        <title></title>
    </head>
    <body>
        <div id="js-console">
        </div>
        <script src="scripts/js-console.js"></script>
        <script>
            //Write a JavaScript function that finds how many times a substring is contained in a 
            //given text (perform case insensitive search).
            //    Example: The target substring is "in". The text is as follows:
            //      We are living in an yellow submarine. We don't have anything else. Inside the submarine 
            //      is very tight. So we are drinking all the day. We will move out of it in 5 days.

            function countOfSubstring(text) {
                var index = -1;
                var count = 0;
                while (true) {
                    index = text.toLowerCase().indexOf('in', index + 1);
                    if (index == -1)
                    {
                        break;
                    }
                    count++;
                }
                return count;
            }
            var someText = "We are living in an yellow submarine. We don't have anything else. Inside the submarine is very tight. So we are drinking all the day. We will move out of it in 5 days."
            jsConsole.writeLine(countOfSubstring(someText));
        </script>
    </body>
</html>
